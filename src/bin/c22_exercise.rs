pub fn sum_of_squares(nums: &[i32]) -> i32 {
    // TODO: Return the sum of squares of every item in `nums`.
    // Hint: .iter().map(|&n| n * n).sum()
    let _ = nums;
    0
}

fn main() {
    println!("{}", sum_of_squares(&[1, 2, 3, 4]));
}
